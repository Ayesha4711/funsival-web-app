import PreferenceSelectionPage from "@/components/features/PreferenceSelectionPage";

export const metadata = {
  title: "Your Preferences | Funsival",
  description: "Select your interests in amenities, equipment, and services.",
};

export default function Preferences() {
  return <PreferenceSelectionPage />;
}
