import AddListingWizard from "@/components/dashboard/listings/AddListingWizard";

export const metadata = {
  title: "Add New Listing | Funsival",
  description: "Create a new activity listing",
};

export default function AddListingPage() {
  return <AddListingWizard />;
}
